<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oracle.S202350102.MainUser1Mapper">
	<select id="level1List" resultType="Level1">
		SELECT * FROM level1
	</select>
	
	<select id="level1Select" parameterType="int" resultType="Level1">
		SELECT * FROM level1 WHERE user_level = #{user_level}
	</select>

</mapper>